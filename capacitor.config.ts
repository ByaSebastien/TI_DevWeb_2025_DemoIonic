import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TI_DevWeb_2025_DemoIonic',
  webDir: 'www'
};

export default config;
